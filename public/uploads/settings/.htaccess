# Allow access to uploaded files
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Prevent PHP execution in upload directory
<FilesMatch "\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
